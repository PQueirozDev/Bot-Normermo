providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22", "ffmpeg", "fontconfig", "dejavu_fonts"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run railway:start"
